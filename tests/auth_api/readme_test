1 Как запускать тесты
Запускать также как раньше - docker-compose build далее docker-compose up

Это в auth_api для запуска сервера и те же команды в tests/auth_api для запуска тестов

После запуска докеров выполнить инициализацию базы (1 раз) потом остановить контейнеры и потом запустить снова
psql --username=postgres --host=127.0.0.1 --port=5432 postgres < db_schema.sql+


ВОПРОСЫ ПО КОДУ ДРУГОГО УЧАСТНИКА
1. в файле :tests/functional/src/test_role.py тестовые данные заливаются через функцию get_or_create но я не
нашел нигде ни импорта этой функции ни ее самой:
user, _ = get_or_create(User, **user_payload)

2. В проекте используется alembic  - почему нет его у Нас и для чего он у них?
3. Все что касается работы с соц сетями в папке services - но там просто методы , но я не нашел их применения?
Также я не нашел там env файла из которого core.config берет данные для VK например - вот эти:
from core.config import (
    VK_API_URL,
    VK_API_VERSION,
    VK_AUTHORIZE_PARAMS,
    VK_AUTHORIZE_URL,
    VK_CLIENT_ID,
    VK_CLIENT_SECRET,
    VK_GET_ACCESS_TOKEN_URL,
    VK_REDIRECT_URI,
)


Вопрросы по след спринту:
1.Логер
2. ЕTL
3. LOGSTASH
4. пути директорий в файлах юпитер
5. Как делали тесторирование в юпитере  -сами или этот код или пример уже где то есть?
6. Файлы в нод файлах по этому пути как получаются  ? ugc_sprint_2_new/services/clickhouse/
7. Как работет пре коммит файл
8. В make_file как раз запускаются 2 разных контейнера и рахных приложения ? ugc_sprint_2_new/Makefile (уже обсуждали но на всякий случай)
но для приложения которое запускается вторым комманда с f docker-compose -f ./elk/docker-compose.yml up -d --build  - что она значит?
9. Для чего и что делает это файл : ugc_sprint_2_new/design.puml ?
10.
